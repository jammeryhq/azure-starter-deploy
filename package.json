{
  "name": "typescript-action",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript template action",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/**/*.ts --fix",
    "pack": "ncc build",
    "all": "npm run build && npm run lint && npm run pack"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/actions/typescript-action.git"
  },
  "author": "YourNameOrOrganization",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.4"
  },
  "devDependencies": {
    "@types/node": "^14.0.23",
    "@zeit/ncc": "^0.22.3",
    "eslint-config-jammeryhq-node-ts": "^1.0.3",
    "typescript": "^3.9.7"
  }
}
